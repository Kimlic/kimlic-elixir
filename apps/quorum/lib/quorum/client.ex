defmodule Quorum.Client do
  def create_user_account(params) do
    # ToDo: here will be JSON.RPC method call to Quorum BlockChain
  end

  def create_verification_contract(params) do
    # ToDo: here will be JSON.RPC method call to Quorum BlockChain
  end

  def update_user_account(params) do
    # ToDo: here will be JSON.RPC method call to Quorum BlockChain
  end
end
